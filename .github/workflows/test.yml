name: Test GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Hola Mundo
        run: echo "GitHub Actions funciona correctamente"